/*
  @ Number
    $ Primitive Type

    $ Number()

    $ NaN

    $ +Infinity

    $ -Infinity

    $ Decimal

    * isNaN
      $ it checks that the passed value is NaN and its type is Number

      const str = "we";

      isNaN(str) // true

    * Number.isNaN
      const nan = NaN;

      Number.isNaN(nan)  // true

    * isFinite()
      $ it checks that a number is neither positive nor negative Infinity
        Number.isFinite(0 / 0) // false

        Number.isFinite(10 / 5) // true

    * isInteger()
      $ Number.isInteger(9)

    * parseInt()
      $ parseInt(10)

    * parseFloat()
      $ parseFloat(10.23)

    * isSafeInteger()
      $ Number.isSafeInteger(11)

    * toString()

    * valueOf()
*/
